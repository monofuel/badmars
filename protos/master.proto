syntax = "proto3";

package masterService;

message Host {
	string hostname = 1;
}
message RegisterReply {
	bool success = 1;
}

service Command {
	rpc RegisterHost(Host) returns (RegisterReply) {}
}
