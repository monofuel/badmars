apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    name: bm-ai-dev
  name: bm-ai-dev
spec:
  replicas: 1
  selector:
    name: bm-ai-dev
  template:
    metadata:
      labels:
        name: bm-ai-dev
    spec:
      containers:
      - image: badmars/nodejs-dev:v2
        name: bm-ai-dev
        command:
         - node
        args:
         - /mnt/src/github.com/monofuel/badMars-JS/server/ai.js
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 7005
          name: bm-ai-dev
        volumeMounts:
          - name: nfs
            mountPath: "/mnt"
      volumes:
      - name: nfs
        persistentVolumeClaim:
          claimName: nfs
